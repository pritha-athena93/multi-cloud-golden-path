apiVersion: v2
name: vault
description: HashiCorp Vault with KMS auto-unseal and Kubernetes auth
type: application
version: 0.1.0
dependencies:
  - name: vault
    version: "0.27.0"
    repository: "https://helm.releases.hashicorp.com"
